
## 2026-05-23 08:40 UTC — pre-compaction flush

- Dan asked to remove all memory related to “karma farming”; prior assistant edited memory accordingly and confirmed completion. Keep future references neutral and avoid resurrecting deleted karma-farming context.
- Dan challenged why Reddit posting/comment workflow worked last week but is now blocked/refused. Important current context: he wants to understand/repeat prior automation and asked about rollback or using another LLM to automate fully.
- Conversation shifted to local/open-source LLM testing for OpenClaw/GX10. Dan asked which capable open-source agent models can run on GX10, then specifically searched for abliterated variants on Hugging Face, including Qwen3.6-27B-FP8 and NVFP4 abliterated.
- Current known GX10 model context from MEMORY.md remains relevant: local vLLM Qwen3.6-27B and Gemma-4-31B provider setup; Dan observed the installed Qwen model performed better than 35B and asked whether anything better has appeared.

## 2026-05-23 09:18 UTC — GX10 Qwen model swap for testing

- Dan first considered `prithivMLmods/Q3.6-27B-GLM-5.1-DA-nvfp4`, then stopped and selected `lyf/Qwen3.6-27B-uncensored-heretic-v2-NVFP4-MTP` for testing.
- GX10 `qwen36` Docker container was recreated from image `vllm/vllm-openai:v0.17.1-cu130` with the new HF model, keeping served model name `qwen3.6-27b`, external endpoint `http://enkzoned.go.ro:12321/v1`, internal port `8000`, `max_model_len=65536`, `gpu_memory_utilization=0.45`, tool parser `qwen3_coder`, reasoning parser `qwen3`, prefix caching, and the same vLLM API credential.
- Verification: `/v1/models` returns root `lyf/Qwen3.6-27B-uncensored-heretic-v2-NVFP4-MTP`; `/v1/chat/completions` works. This model emits a separate `reasoning` field, so tests should inspect both `message.content` and `message.reasoning`.
- Dan asked to put this model on FORGE. Persistent config changed: `/data/.openclaw/openclaw.json` agent `forge` primary is now `vllm-qwen/qwen3.6-27b`; `/data/.openclaw/workspace/agents/coding-team/FORGE.md` and `agents.json` were updated to match. Active session `agent:forge:main` was also switched via `session_status`.
- Dan noticed the Telegram direct FORGE session was still on Kimi. Corrected `agent:forge:telegram:direct:387564627` to `vllm-qwen/qwen3.6-27b` via `session_status`; `sessions_list agentId=forge` now shows both `agent:forge:main` and the Telegram direct FORGE session on `qwen3.6-27b`. An old completed FORGE subagent remains `gpt-5.5`, but it is historical/done.
- Qwen integration issue found: with vLLM launched using `--reasoning-parser qwen3`, the selected `lyf/...NVFP4-MTP` model often returned `message.content=null` and put the answer in `message.reasoning`, making OpenClaw report “Agent couldn't generate a response.” Recreated `qwen36` without `--reasoning-parser qwen3`; `/v1/chat/completions` now returns normal `message.content`, and `sessions_send` smoke on `agent:forge:main` replied `Model activ: vllm-qwen/qwen3.6-27b`.

## 2026-05-23 10:44 UTC — Main/Nox switched to local Qwen

- Dan asked “Treci si tu pe Qwen”. Updated persistent `/data/.openclaw/openclaw.json` agent `main` model to `vllm-qwen/qwen3.6-27b` with no configured fallbacks.
- Current Telegram direct session `agent:main:telegram:direct:387564627` has `providerOverride=vllm-qwen`, `modelOverride=qwen3.6-27b`; `sessions_list agentId=main` reports the session model as `qwen3.6-27b`.
- Caveat: the in-flight Codex rollout can still report `openai-codex/gpt-5.5` until the next turn/reload; effective session metadata is already Qwen for the next generated response.
