# 2026-03-17 — Memory Log

## Phone Network v2.3.0 Deployed

**Build & OTA** (08:19-08:25 UTC):
- Built APK with WiFi Watchdog feature
- Java found at `/home/linuxbrew/.linuxbrew/Cellar/openjdk@17/17.0.18/libexec` (needed `JAVA_HOME` export)
- OTA pushed to 2 devices successfully

**Device Status post-OTA:**
- `op5t3`: online, v2.3.0-debug ✅
- `op5t2`: online, v2.3.0-debug ✅
- `op5t1`: offline, v2.2.8-debug (didn't receive OTA)

**v2.3.0 Features:**
- WiFi Watchdog: auto toggle WiFi OFF/ON after 2min disconnect
- Watchdogs: connecting (45s), auth (30s), PONG timeout (60s)
- Read timeout: 90s

**Disconnection Investigation** (08:35-08:41 UTC):
- Dan reported op5t3 disconnected after OTA
- Cause: normal OTA restart behavior (app closes for install, then reconnects)
- Device reconnected successfully — no actual issue

## Build Command Reference
```bash
export JAVA_HOME=/home/linuxbrew/.linuxbrew/Cellar/openjdk@17/17.0.18/libexec
cd /data/.openclaw/workspace-volt/phone-network/android-agent
./gradlew assembleDebug
# Output: app/build/outputs/apk/debug/app-arm64-v8a-debug.apk
```

---

## Nautilus Pipeline Setup (12:44-13:05 UTC)

**Endpoints adăugate:**
- `POST /api/nautilus/run` — rulează pipeline-ul manual
- `GET /api/nautilus/status` — verifică status marketing

**Cron creat:**
- `nautilus-nightly` @ 02:30 Europe/Bucharest (zilnic)
- Channel: telegram, timeout: 5min

**DB Schema Fixes aplicate:**
- `execution_logs`: +account_id, +task_type, +success, +error, +created_at
- `materials`: +thumbnail_url, +metadata, +tags, +used_at, +created_at
- `reports`: +period_start, +period_end, constraint fix pentru 'daily_performance'
- `marketer_escalations`: +response, +resolved_at

**Bug fix Marketer:**
- ON CONFLICT reseta status la 'pending' — fixat să nu mai reseteze escalări answered

**Client Incitographer:**
- Strategy setat cu peak_hours: 08-10, 12-14, 18-21
- Comment mode: contextual (nu templates fixe)
- Comment language: auto-detect (default română)

**Pipeline Flow:**
```
Marketer (creează strategy) → Tactician (planifică tasks) → Queue → Hydra (execută)
```

**Task-uri generate:**
- 10 engage_session tasks în queue pentru incitographer
- Target: #relevant1 (placeholder - trebuie actualizat cu hashtag-uri reale)
- Device: op5t2
