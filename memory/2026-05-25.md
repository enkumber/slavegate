# 2026-05-25 — Note Phone Network

## Conversație arhitectură (06:22 → 11:18 UTC, 57 mesaje, 430KB)
### Discuție: Nox + Dan

**1. Modele pe GX10:**
- Qwen3.6-27B dense (deja există) — vision nu, context 32K-64K
- MoE — Qwen3.6-27B, Qwen3.5-32K, Gemma-4-31B
- Qwen3.5-AWQ-Qwen3.6-32B-ultralight (4.8GB)
- Decizie: lăsăm așa momentan

**2. Arhitectura workflow-urilor:**
- Format JSON: `id`, `name`, `app`, `device_info`, `account`, `client`, `steps[]`
- `device_info`: id, model, screen_width, screen_height, resolution
- `steps`: open_app, tap, type, wait, scroll, screenshot (action+params)
- Coordonate normalizate (ratio 0-1), se scalează per device în runtime
- Compilare pe server (LLM are context complet)
- Workflow generat stă pe server, device descarcă din queue

**3. Queue & Device mapping:**
- Device-based queue per device
- Hierarchy: client → conturi social media → device (1 cont = 1 device specific)
- Dashboard: „Pe ce device salvezi acest cont?"

**4. Communication:**
- Full streaming pentru faza inițială (vedem tot ce se întâmplă pe device)
- Format compact, stream continuu

**5. Workflow creative:**
- Agent marketing analizează contul clientului, nevoile, social media
- Propune workflow în limbaj uman → compilat în JSON steps pe server

**6. Task:** Verifică ce e implementat vs. ce nu

## Status curent
- Fișiere verificate: `workflow-compiler/types.ts`, `workflows/types.ts`
- Există: `Workflow`, `WorkflowStep`, `StepAction`, `StepTarget`, `DeviceQueue`, `DeviceAssignment`
- Următor pas: verifică task-runner, agency controller, client→account→device mapping

## Task Phone Network (15:00+)
- ATLAS a creat story: `STORY-PN-STREAMING-CREATIVE-E2E-001.md`
- Gap-urile identificate:
  - Full streaming (device → server → dashboard real-time)
  - Flux creative E2E (agent marketing → workflow JSON → execution → report)

**Goal 1 — Full Streaming (DONE)**
- VOLT: `modules/workflow-events/` creat + publishing sites actualizate
- ECHO review: APPROVED, 30 teste
- JWT WS auth, WS token subprotocol, payload cap, step semantics, reconnect/resync
- Test coverage: 30 teste relevante, 252 teste totale, build OK

**Goal 2 — Creative E2E (în progres)**
- Flux: agent marketing → workflow JSON → execution → report
- Story definit, arhitectură FORGE, task breakdown în proces
