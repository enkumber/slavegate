# 2026-03-27

## Phone Network — Task Runner Implementat

**FORGE + VOLT** au implementat task-runner service:
- `src/modules/task-runner/task-runner.service.ts` (481 linii)
- Migration 022 pentru coloane `result`, `error`
- API: `/api/task-runner/start|stop|status|execute/:taskId`
- Poll interval: 30s, device lock, rate limit 60s gap

**Problemă descoperită:** Task-urile @incitographer (27 total) au trecut toate în `failed` din cauza Anthropic API overloaded. Task runner-ul **nu reîncearcă** task-urile failed — doar preia din `queued`.

**TODO:** 
- [ ] Retry logic pentru failed tasks (sau reset manual la queued)
- [ ] Nautilus timeout (5 min) — neadresat încă

## Git Local Inițializat

- Repo: `/data/.openclaw/workspace-kraken/phone-network-server/.git`
- Initial commit: `bff0878` — 164 files, 36,685 lines
- Regula comunicată la FORGE: commit obligatoriu după fiecare task
- Convenții: `feat:`, `fix:`, `refactor:`, `cleanup:`, `docs:`

## Coordonate nav bar Instagram (confirmat)

- y=0.912 (corect pentru toate device-urile)
- Home (0.10), Reels (0.30), Search (0.50), Create (0.70), Profile (0.90)
- Threshold unificat: 0.85

## Decizii

- Versioning: doar local git (fără GitHub/GitLab pentru moment)
- Deploy cleanup: completat, server restartat, 4 device-uri online

## OpenAI OAuth Setup (în progres)

**Context:** Anthropic API instabil pentru imagini mari (screenshot-uri). Dan vrea OpenAI GPT-4o ca fallback.

**Dan are ChatGPT Plus** — vrea OAuth, nu API key separat.

**Comandă necesară (pe server 192.168.50.57):**
```bash
openclaw models auth login --provider openai
```

**Status:** Dan trebuie să ruleze comanda în terminal interactiv (necesită TTY).

**După completare:** Modific `llm-client.ts` să folosească GPT-4o pentru vision ca fallback.

## Ollama Cloud Models — Testare Qwen3.5 (14:10-15:10 UTC)

**Testat modele locale pe PC (192.168.50.185, RTX 4070 Super 12GB VRAM):**
- `qwen3.5:9b` — funcționează pentru JSON simplu, dar crash-uiește cu imagini mari (500 error)
- `qwen3:14b` — funcționează stabil pentru planning (56s per request)
- `llava:34b` — vision OK dar ocupă 10GB VRAM, nu lasă loc pentru Qwen

**Problemă:** LLaVA 34b + Qwen3:14b nu încap simultan în 12GB VRAM

**Modele cloud disponibile pe Ollama:**
- `qwen3-vl:235b-instruct-cloud` — **multimodal, recomandat pentru vision+planning**
- `qwen3-next:80b-cloud` — text only
- `minimax-m2.7:cloud` — MiniMax API

**Decizie Dan (14:29):** Folosim `qwen3-vl:235b-instruct-cloud` pentru vision+planning

**Cod modificat (nedeployat încă):**
- `llm-client.ts` — routing pentru Ollama vision + planning
- `planner.agent.ts` — model setat la qwen3.5:4b (trebuie schimbat la cloud)

**TODO (imediat):**
- [ ] Setează `qwen3-vl:235b-instruct-cloud` în cod
- [ ] Build + deploy
- [ ] Test task runner

## Task Runner Testing Session (15:20-16:05 UTC)

**Revert la Anthropic** — Qwen cloud avea probleme, revenit la Anthropic.

**Config:**
- `USE_LOCAL_VISION=false`, `USE_LOCAL_PLANNING=false`
- Model: `anthropic/claude-sonnet-4-6`

**Progres testat:**
- ✅ Open Instagram (preamble OK)
- ✅ Tap Search tab (nav.search la 0.50, 0.912)
- ✅ Tap Search bar (search.search_bar la 0.50, 0.07)
- ✅ Type hashtag (#photography, #streetphotography)
- ❌ Tap hashtag result — coordonate greșite, cade pe cont în loc de hashtag
- ✅ Like post (funcționează când ajunge pe post)
- ❌ Navigate next post — LLM face plan cu "swipe left" în loc de scroll vertical

**Probleme descoperite:**

1. **Skill templates nu se copiază automat în `dist/`** — trebuie copiere manuală după fiecare build:
   ```bash
   cp src/modules/skills/templates/*.skill dist/modules/skills/templates/
   ```

2. **Accessibility tree returnează coordonate greșite** — nav bar la y=0.969 (zona gesture Android), trebuie y=0.912

3. **LLM face planuri cu acțiuni greșite** — "swipe left to next post" nu funcționează în Instagram, trebuie scroll vertical

**Fix-uri aplicate:**
- Adăugat `search.search_bar` în button_map (selector + learned_coords)
- Adăugat reguli Instagram-specifice în planner prompt (scroll, nu swipe)
- Task-uri vechi marcate "paused" (format incompatibil cu codul nou)

**Stare finală:** Task runner **OPRIT** la cererea lui Dan. Toate task-urile pe pauză.

**Următorii pași:**
- [ ] Automatizare copiere templates în build script
- [ ] Fix coordonate hashtag result (skill learning sau prompt tuning)
- [ ] Test cu hashtag mai specific (mai puține rezultate de conturi)
