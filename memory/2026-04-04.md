# 2026-04-04

## Phone Network Umbrel Deployment — Progress

### Completed (2026-04-03 evening)
- **Docker image on GHCR**: `ghcr.io/enkumber/slavegate-server:latest` — public, builds via GitHub Actions
- **Umbrel app installed**: `slavegate-phone-network` — installs successfully
- **Database**: PostgreSQL connected, all migrations applied (schema + Nostr tables + incremental)
- **Nostr keypair generated**: pubkey `a58f863dfefa2397e66ac5064ac40366bcd3490f5a3bbed23494431cf7a3d630`

### Issues Fixed Along the Way
- `psql: command not found` → added `postgresql-client` to Dockerfile
- `CREDENTIAL_ENCRYPTION_KEY` duplicated in docker-compose → cleaned up
- `nostr-relay_default` network not found → switched to `umbrel_main_network` (external)
- `POSTGRES_PASSWORD` not expanding (nested `${}`) → hardcoded credentials in compose
- Postgres data dir owned by root → `chmod 777`
- `Cannot find module dist/phone-network-server/src/index.js` → fixed entrypoint path + tsconfig rootDir
- Container naming changed from `_` to `-` separators in newer docker compose

### Current Blocker (2026-04-04)
- **Server up 12h but HTTP not responding** — `Connection reset by peer` on `curl localhost:3000`
- Root cause: **Redis not configured** — server logs show `[redis] Connection error (throttled, 1/min)` continuously
- Server likely hangs during bootstrap waiting for Redis, Express never starts listening
- **Fix pushed**: Added `redis:7-alpine` service to docker-compose with `REDIS_URL=redis://phone-network-redis:6379`
- Dan needs to pull new compose and recreate containers

### Key Config Values (Umbrel)
- **API_KEY**: `24018503cf6bd0ae1e5b1f829a5f66cf23fc2160aea90dff3b91427419cec746`
- **JWT_SECRET**: `88e43882eb6af62fc45ee602ed1b2d91789e81e695071c7e0740587c7b12023f`
- **CREDENTIAL_ENCRYPTION_KEY**: `d3b10ccdd00f9c5fff603deeb009f9e893a53b0a2781a0ee01740ade8c838c52`
- **Dashboard**: admin / `62fa93907efb78288aaa826d0a8dd6f1`
- **DB**: `phoneNetwork:phoneNetwork_umbrel@phone-network-db:5432/phoneNetwork`
- **Nostr Relay**: `ws://nostr-relay_relay_1:8080` (actual port from config.toml)

### Umbrel Docker Network
- All Umbrel apps share `umbrel_main_network` — no per-app networks
- Nostr relay containers: `nostr-relay_relay_1` (port 8080), `nostr-relay_web_1` (Caddy, port 80), `nostr-relay_relay-proxy_1`

### GitHub Repos
- **Public (Umbrel store)**: `enkumber/slavegate` — docker-compose, umbrel-app.yml
- **Private (server source)**: `enkumber/slavegate-server` — Dockerfile, GitHub Actions for GHCR
- GitHub CDN caches raw.githubusercontent.com ~5min; use API endpoint with `Accept: application/vnd.github.raw` header to bypass

### Next Steps
- [ ] Dan pulls new docker-compose with Redis and restarts
- [ ] Verify server responds on HTTP after Redis available
- [ ] Verify Nostr relay connection
- [ ] Test enrollment API
- [ ] Build Android APK 3.0.0 and deploy to devices
- [ ] End-to-end test: enroll device → heartbeat → command execution

## Update 08:26 UTC — Server LIVE

### Redis Fix Worked
- Added `redis:7-alpine` container → server now passes bootstrap
- Server fully starts: parser-registry, skill-updater, ops-monitor, Nostr transport all init

### New Nostr Keypair
- After Redis fix, server generated NEW keypair: `32f8ea92...` (previous was `a58f863d...`)
- This is because the old key was in the old container's `/data` — need to verify persistence

### Non-fatal Error
- `ops-monitor`: `column "timestamp" does not exist` — likely a schema mismatch in ops_monitor/heartbeat table
- Need migration to add `timestamp` column or fix the query
- Server continues running despite this error

### Waiting On
- Dan's response to `curl localhost:3000/api/devices` test
- If API responds, server is fully operational and ready for device enrollment
# 2026-04-04

## Phone Network Umbrel Deployment — Progress

### Completed (2026-04-03 evening)
- **Docker image on GHCR**: `ghcr.io/enkumber/slavegate-server:latest` — public, builds via GitHub Actions
- **Umbrel app installed**: `slavegate-phone-network` — installs successfully
- **Database**: PostgreSQL connected, all migrations applied (schema + Nostr tables + incremental)
- **Nostr keypair generated**: pubkey `a58f863dfefa2397e66ac5064ac40366bcd3490f5a3bbed23494431cf7a3d630`

### Issues Fixed Along the Way
- `psql: command not found` → added `postgresql-client` to Dockerfile
- `CREDENTIAL_ENCRYPTION_KEY` duplicated in docker-compose → cleaned up
- `nostr-relay_default` network not found → switched to `umbrel_main_network` (external)
- `POSTGRES_PASSWORD` not expanding (nested `${}`) → hardcoded credentials in compose
- Postgres data dir owned by root → `chmod 777`
- `Cannot find module dist/phone-network-server/src/index.js` → fixed entrypoint path + tsconfig rootDir
- Container naming changed from `_` to `-` separators in newer docker compose

### Current Blocker (2026-04-04)
- **Server up 12h but HTTP not responding** — `Connection reset by peer` on `curl localhost:3000`
- Root cause: **Redis not configured** — server logs show `[redis] Connection error (throttled, 1/min)` continuously
- Server likely hangs during bootstrap waiting for Redis, Express never starts listening
- **Fix pushed**: Added `redis:7-alpine` service to docker-compose with `REDIS_URL=redis://phone-network-redis:6379`
- Dan needs to pull new compose and recreate containers

### Key Config Values (Umbrel)
- **API_KEY**: `24018503cf6bd0ae1e5b1f829a5f66cf23fc2160aea90dff3b91427419cec746`
- **JWT_SECRET**: `88e43882eb6af62fc45ee602ed1b2d91789e81e695071c7e0740587c7b12023f`
- **CREDENTIAL_ENCRYPTION_KEY**: `d3b10ccdd00f9c5fff603deeb009f9e893a53b0a2781a0ee01740ade8c838c52`
- **Dashboard**: admin / `62fa93907efb78288aaa826d0a8dd6f1`
- **DB**: `phoneNetwork:phoneNetwork_umbrel@phone-network-db:5432/phoneNetwork`
- **Nostr Relay**: `ws://nostr-relay_relay_1:8080` (actual port from config.toml)

### Umbrel Docker Network
- All Umbrel apps share `umbrel_main_network` — no per-app networks
- Nostr relay containers: `nostr-relay_relay_1` (port 8080), `nostr-relay_web_1` (Caddy, port 80), `nostr-relay_relay-proxy_1`

### GitHub Repos
- **Public (Umbrel store)**: `enkumber/slavegate` — docker-compose, umbrel-app.yml
- **Private (server source)**: `enkumber/slavegate-server` — Dockerfile, GitHub Actions for GHCR
- GitHub CDN caches raw.githubusercontent.com ~5min; use API endpoint with `Accept: application/vnd.github.raw` header to bypass

## Update 08:26 UTC — Server LIVE

### Redis Fix Worked
- Added `redis:7-alpine` container → server now passes bootstrap
- Server fully starts: parser-registry, skill-updater, ops-monitor, Nostr transport all init

### New Nostr Keypair
- After Redis fix, server generated NEW keypair: `32f8ea92...` (previous was `a58f863d...`)
- This is because the old key was in the old container's `/data` — need to verify persistence

### Non-fatal Errors
- `ops-monitor`: `errorMissingColumn` at position 76 — schema mismatch
- `canary_rollouts` table does not exist — canary feature not yet migrated

## Update 12:00-13:18 UTC — Dashboard + Relay Migration

### Dashboard Working
- Dashboard included in Docker image (commit `9a61e8d`)
- Dashboard base URL was hardcoded to `https://relay.pozesexy.com/api` — fixed via sed in container
- Fix: replaced all `relay.pozesexy.com` references with relative `/api` paths using docker cp + sed
- Dashboard accessible at **http://192.168.50.57:3000** — login works

### Relay Migration: Local → Public
- **Decision**: Move from local Umbrel Nostr relay to public relays
- **Public relays**: `wss://relay.damus.io` + `wss://nos.lol`
- Umbrel local relay (`ws://nostr-relay_relay_1:8080`) kept as optional backup but not primary
- Docker-compose updated with `NOSTR_RELAY_PUBLIC` env var
- Server subscribes on public relays now (confirmed: `Subscription caught up EOSE`)

### Android Auto-Discovery (No Enrollment)
- Created `NostrConfig.kt` — hardcoded server pubkey + public relay URLs
- App connects automatically without QR/manual enrollment
- Flow: install APK → auto-connect → DEVICE_HELLO → appears in dashboard as pending → admin approves
- Server pubkey: `32f8ea9266125773e9100496d9744ca827379caa09f55593b5e649d480cbaf9d`

### Dashboard URL Fix (Important for Future)
- `dashboard-dist/` JS files contain hardcoded base URL from build time
- Current fix is sed in container — **needs permanent fix in Dockerfile** (sed during build or env-based config)
- Multiple JS files in `/app/dashboard-dist/assets/` — only `index-BqAtFaVH.js` is referenced by `index.html`

### Status at 13:18 UTC
- ✅ Server running on Umbrel, connected to public relays
- ✅ Dashboard accessible and login works
- ⏳ Waiting for Android phone to connect via new APK (auto-discovery)
- Phone has new APK installed but no DEVICE_HELLO received yet — may need app restart or connectivity check

### Open Issues
- [ ] Verify Android phone connects to public relays and sends DEVICE_HELLO
- [ ] Dashboard base URL fix needs to be baked into Dockerfile (not manual sed)
- [ ] `errorMissingColumn` at position 76 — ops-monitor query issue
- [ ] `canary_rollouts` table missing
- [ ] Clean up old test devices in DB
- [ ] Full end-to-end test: auto-discover → approve → heartbeat → command execution

### Git Commits Today
- `9a61e8d` feat: include dashboard in Docker image + fix dashboard path + log full pubkey
- (+ docker-compose updates for public relay)
- `810d2c2` fix: use correct jobId in sendJob + save JOB_RESULT to DB
- `5380ff7` feat: migration to expand job_type constraint
- `13fac77` fix: waitForJobResult uses adapter promise instead of DB polling
- `238f7a7` fix: remove job_type constraint entirely
- `488e16a` feat: use DDNS enkzoned.go.ro for relay public URL
- `11a0f48` feat: add local Umbrel relay as primary for LAN speed

### Critical Bugs Fixed (afternoon session)
1. **sendJob ignora job.jobId** — genera alt UUID intern, telefonul răspundea cu ID greșit → "unknown job"
2. **JOB_RESULT nu updata DB** — `waitForJobResult` poll-a DB dar handler nu scria acolo (TODO Sprint 3)
3. **waitForJobResult polling lent** — adăugat `registerWaiter()` pe adapter (Promise-based, nu DB polling)
4. **jobs_job_type_check constraint** — blocant la fiecare job type nou. Eliminat permanent.

### RustDesk Enable — FUNCȚIONEAZĂ! ✅
- Flow complet: Preamble → Open App → Share Screen → Status Check → Warning Dialog → Capture Dialog → System Dialog → Verify → Success
- Problema: ~8-10s per step cu relay public (relay.damus.io)

### Viteză Nostr — Problemă activă
- **Cauza:** relay public adaugă ~3-5s latență per hop (server→relay→telefon→relay→server)
- **Încercat:** push mode (subscribe + handleNotifications) în loc de polling — nu a ajutat semnificativ
- **Încercat:** relay local Umbrel (`ws://nostr-relay_relay_1:8080`) expus pe port 4848, DDNS `enkzoned.go.ro:4848`
- **Status:** Dan zice tot lent. Trebuie investigat mai departe.
- **Next:** verifică dacă relay local funcționează + dacă Android chiar folosește subscribe/push

### Relay Config
- **Server:** primary=`ws://nostr-relay_relay_1:8080` (local Docker), secondary=`wss://relay.damus.io`, public=`ws://enkzoned.go.ro:4848`
- **Android:** primary=`ws://enkzoned.go.ro:4848`, fallback=`wss://relay.damus.io`
- Port forward necesar: `4848` TCP pe router → `192.168.50.57:4848`
- DDNS: `enkzoned.go.ro`
# 2026-04-04

## Phone Network Umbrel Deployment — Progress

### Completed (2026-04-03 evening)
- **Docker image on GHCR**: `ghcr.io/enkumber/slavegate-server:latest` — public, builds via GitHub Actions
- **Umbrel app installed**: `slavegate-phone-network` — installs successfully
- **Database**: PostgreSQL connected, all migrations applied (schema + Nostr tables + incremental)
- **Nostr keypair generated**: pubkey `a58f863dfefa2397e66ac5064ac40366bcd3490f5a3bbed23494431cf7a3d630`

### Issues Fixed Along the Way
- `psql: command not found` → added `postgresql-client` to Dockerfile
- `CREDENTIAL_ENCRYPTION_KEY` duplicated in docker-compose → cleaned up
- `nostr-relay_default` network not found → switched to `umbrel_main_network` (external)
- `POSTGRES_PASSWORD` not expanding (nested `${}`) → hardcoded credentials in compose
- Postgres data dir owned by root → `chmod 777`
- `Cannot find module dist/phone-network-server/src/index.js` → fixed entrypoint path + tsconfig rootDir
- Container naming changed from `_` to `-` separators in newer docker compose

### Current Blocker (2026-04-04)
- **Server up 12h but HTTP not responding** — `Connection reset by peer` on `curl localhost:3000`
- Root cause: **Redis not configured** — server logs show `[redis] Connection error (throttled, 1/min)` continuously
- Server likely hangs during bootstrap waiting for Redis, Express never starts listening
- **Fix pushed**: Added `redis:7-alpine` service to docker-compose with `REDIS_URL=redis://phone-network-redis:6379`
- Dan needs to pull new compose and recreate containers

### Key Config Values (Umbrel)
- **API_KEY**: `24018503cf6bd0ae1e5b1f829a5f66cf23fc2160aea90dff3b91427419cec746`
- **JWT_SECRET**: `88e43882eb6af62fc45ee602ed1b2d91789e81e695071c7e0740587c7b12023f`
- **CREDENTIAL_ENCRYPTION_KEY**: `d3b10ccdd00f9c5fff603deeb009f9e893a53b0a2781a0ee01740ade8c838c52`
- **Dashboard**: admin / `62fa93907efb78288aaa826d0a8dd6f1`
- **DB**: `phoneNetwork:phoneNetwork_umbrel@phone-network-db:5432/phoneNetwork`
- **Nostr Relay**: `ws://nostr-relay_relay_1:8080` (actual port from config.toml)

### Umbrel Docker Network
- All Umbrel apps share `umbrel_main_network` — no per-app networks
- Nostr relay containers: `nostr-relay_relay_1` (port 8080), `nostr-relay_web_1` (Caddy, port 80), `nostr-relay_relay-proxy_1`

### GitHub Repos
- **Public (Umbrel store)**: `enkumber/slavegate` — docker-compose, umbrel-app.yml
- **Private (server source)**: `enkumber/slavegate-server` — Dockerfile, GitHub Actions for GHCR
- GitHub CDN caches raw.githubusercontent.com ~5min; use API endpoint with `Accept: application/vnd.github.raw` header to bypass

## Update 08:26 UTC — Server LIVE

### Redis Fix Worked
- Added `redis:7-alpine` container → server now passes bootstrap
- Server fully starts: parser-registry, skill-updater, ops-monitor, Nostr transport all init

### New Nostr Keypair
- After Redis fix, server generated NEW keypair: `32f8ea92...` (previous was `a58f863d...`)
- This is because the old key was in the old container's `/data` — need to verify persistence

### Non-fatal Errors
- `ops-monitor`: `errorMissingColumn` at position 76 — schema mismatch
- `canary_rollouts` table does not exist — canary feature not yet migrated

## Update 12:00-13:18 UTC — Dashboard + Relay Migration

### Dashboard Working
- Dashboard included in Docker image (commit `9a61e8d`)
- Dashboard base URL was hardcoded to `https://relay.pozesexy.com/api` — fixed via sed in container
- Fix: replaced all `relay.pozesexy.com` references with relative `/api` paths using docker cp + sed
- Dashboard accessible at **http://192.168.50.57:3000** — login works

### Relay Migration: Local → Public
- **Decision**: Move from local Umbrel Nostr relay to public relays
- **Public relays**: `wss://relay.damus.io` + `wss://nos.lol`
- Umbrel local relay (`ws://nostr-relay_relay_1:8080`) kept as optional backup but not primary
- Docker-compose updated with `NOSTR_RELAY_PUBLIC` env var
- Server subscribes on public relays now (confirmed: `Subscription caught up EOSE`)

### Android Auto-Discovery (No Enrollment)
- Created `NostrConfig.kt` — hardcoded server pubkey + public relay URLs
- App connects automatically without QR/manual enrollment
- Flow: install APK → auto-connect → DEVICE_HELLO → appears in dashboard as pending → admin approves
- Server pubkey: `32f8ea9266125773e9100496d9744ca827379caa09f55593b5e649d480cbaf9d`

### Dashboard URL Fix (Important for Future)
- `dashboard-dist/` JS files contain hardcoded base URL from build time
- Current fix is sed in container — **needs permanent fix in Dockerfile** (sed during build or env-based config)
- Multiple JS files in `/app/dashboard-dist/assets/` — only `index-BqAtFaVH.js` is referenced by `index.html`

### Status at 13:18 UTC
- ✅ Server running on Umbrel, connected to public relays
- ✅ Dashboard accessible and login works
- ⏳ Waiting for Android phone to connect via new APK (auto-discovery)
- Phone has new APK installed but no DEVICE_HELLO received yet — may need app restart or connectivity check

### Open Issues
- [ ] Dashboard base URL fix needs to be baked into Dockerfile (not manual sed)
- [ ] `errorMissingColumn` at position 76 — ops-monitor query issue
- [ ] `canary_rollouts` table missing
- [ ] Clean up old test devices in DB
- [ ] Full end-to-end test: auto-discover → approve → heartbeat → command execution

### Git Commits Today
- `9a61e8d` feat: include dashboard in Docker image + fix dashboard path + log full pubkey
- `810d2c2` fix: use correct jobId in sendJob + save JOB_RESULT to DB
- `5380ff7` feat: migration to expand job_type constraint
- `13fac77` fix: waitForJobResult uses adapter promise instead of DB polling
- `238f7a7` fix: remove job_type constraint entirely
- `488e16a` feat: use DDNS enkzoned.go.ro for relay public URL
- `11a0f48` feat: add local Umbrel relay as primary for LAN speed

### Critical Bugs Fixed (afternoon session)
1. **sendJob ignora job.jobId** — genera alt UUID intern, telefonul răspundea cu ID greșit → "unknown job"
2. **JOB_RESULT nu updata DB** — `waitForJobResult` poll-a DB dar handler nu scria acolo (TODO Sprint 3)
3. **waitForJobResult polling lent** — adăugat `registerWaiter()` pe adapter (Promise-based, nu DB polling)
4. **jobs_job_type_check constraint** — blocant la fiecare job type nou. Eliminat permanent.

### RustDesk Enable — FUNCȚIONEAZĂ! ✅
- Flow complet: Preamble → Open App → Share Screen → Status Check → Warning Dialog → Capture Dialog → System Dialog → Verify → Success
- Problema: ~8-10s per step cu relay public (relay.damus.io)

### Viteză Nostr — Problemă activă
- **Cauza:** relay public adaugă ~3-5s latență per hop (server→relay→telefon→relay→server)
- **Încercat:** push mode (subscribe + handleNotifications) în loc de polling — nu a ajutat semnificativ
- **Încercat:** relay local Umbrel (`ws://nostr-relay_relay_1:8080`) expus pe port 4848, DDNS `enkzoned.go.ro:4848`

## Update 17:00-17:54 UTC — DECIZIE MAJORĂ: Direct WebSocket (Eliminare Nostr)

### Migrare la DirectWs
- **Dan a cerut eliminare completă Nostr** din flow — direct WebSocket telefon→server
- Codul exista deja pe ambele părți (`direct-ws.server.ts` + `DirectWsClient.kt`)
- Transport abstraction layer creat: `src/transport/transport.ts` — DirectWs prioritar
- `TRANSPORT_MODE=direct` în docker-compose
- Android: Nostr complet eliminat din UI + service, doar DirectWsClient

### Enrollment Flow Nou (DirectWs)
- Fără Nostr keys — telefonul trimite fingerprint (UUID generat local, apoi ANDROID_ID)
- Server creează device `pending` → admin aprobă → `approved`
- La reconectări: deviceId + deviceKey salvat în SharedPreferences
- **ANDROID_ID** ales ca identifier final (persistent per device, nu se schimbă la reinstalare)

### DB Schema Issues (Manual Fixes pe Umbrel)
- `device_key` column lipsea → `ALTER TABLE devices ADD COLUMN device_key TEXT UNIQUE`
- `auth_challenges` table lipsea → `CREATE TABLE auth_challenges (...)`
- `first_seen_at` column lipsea → `ALTER TABLE devices ADD COLUMN first_seen_at TIMESTAMPTZ`
- `fingerprint` column adăugată → `ALTER TABLE devices ADD COLUMN fingerprint TEXT UNIQUE`
- **Lesson:** Migrațiile auto nu rulează — trebuie aplicate manual sau adăugate la startup

### APK Deployment Issues
- `docker cp` cu fișiere mari eșuează silent (permission denied pe `/app/apk/`)
- **Fix funcțional:** `docker exec -u root ... wget -O /app/apk/phone-network.apk <github_url>`
- Fișierul vechi era owned de root → trebuie `-u root` la exec

### Status la 17:54 UTC
- ✅ **Telefonul SE CONECTEAZĂ pe DirectWs!** Device `2af36eb6` autenticat
- ✅ Server trimite job-uri (screen_off, ui_tree_dump, screen_wake, unlock, open_app)
- ✅ RustDesk se deschide pe telefon!
- ❌ **JOB_RESULT nu se trimite înapoi** — telefonul execută job dar nu trimite result pe WebSocket
- ❌ Dashboard arată "Failed to open RustDesk app" (timeout pe waitForJobResult)

### Bug Activ: JOB_RESULT lipsă
- Telefonul primește job-ul, îl execută (RustDesk se deschide), dar NU trimite result
- Posibilă cauză: DirectWsClient.kt `executeJob` callback nu trimite result pe WebSocket
- Sau race condition: waiter se registerează DUPĂ sendJob → result vine înainte de waiter
- Fix pushed: `c94ad77` — register waiter before sendJob

### Investigare necesară
- [ ] Verifică `DirectWsClient.kt` sendResult logic — trimite pe WebSocket?
- [ ] Verifică dacă open_app JobExecutor handler apelează callback-ul
- [ ] Rebuild + redeploy cu fix-ul race condition
- [ ] End-to-end test RustDesk cu DirectWs

### Key New Info
- **Device ID nou:** `2af36eb6` (creat prin DirectWs enrollment)
- **IP telefon:** `86.124.202.213` (alt oraș decât Dan)
- **DirectWs endpoint funcțional:** `ws://enkzoned.go.ro:3000/ws-direct`
- **Port forward 3000 activ pe router**
- **Nostr_pubkey: "missing..."** — Android nu trimitea nostr_pubkey (corect, am scos Nostr)

## Update 18:00-18:30 UTC — Nostr Removal Complete + Device Delete Fix

### Nostr COMPLET ELIMINAT din server ✅
- Commit `7dda516` — 20 fișiere, -3119 linii, TSC clean
- `src/nostr/` directory șters complet (adapter, client, encryption, keys, handlers, etc.)
- Toate referințele din routes.ts, hydra-routes.ts, index.ts, transport.ts, orchestrator.ts, skill.cascade.ts, workflow.executor.ts, screen-detection.service.ts — curățate
- Transport: doar DirectWs, zero fallback Nostr

### Device Delete Fix ✅
- `detection_events` table lipsea → cauza crash la DELETE /api/devices/:id ("Empty reply from server")
- `detection_events` table creată manual pe Umbrel
- `deleteDevice()` refactored: toate DELETE-urile din tabele referencing → `.catch(() => {})` (safe even if table missing)
- Route DELETE wrapped în try/catch cu error response
- Commit `cedadd1` (safe delete), apoi `7dda516` (Nostr removal include fix)

### Dashboard Delete "Failed to fetch" — Cauza
- Server crăpa cu uncaught exception la `DELETE FROM detection_events` (table missing)
- Express-ul dădea connection reset ("Empty reply from server")
- Browser interpreta ca "Failed to fetch"
- Fix: safe deletes + try/catch pe rută

### Waiting On
- Dan trebuie `docker compose pull && docker compose down && docker compose up -d` pe Umbrel
- Apoi APK wget + test delete din dashboard
# 2026-04-04

## Phone Network Umbrel Deployment — Progress

### Completed (2026-04-03 evening)
- **Docker image on GHCR**: `ghcr.io/enkumber/slavegate-server:latest` — public, builds via GitHub Actions
- **Umbrel app installed**: `slavegate-phone-network` — installs successfully
- **Database**: PostgreSQL connected, all migrations applied (schema + Nostr tables + incremental)
- **Nostr keypair generated**: pubkey `a58f863dfefa2397e66ac5064ac40366bcd3490f5a3bbed23494431cf7a3d630`

### Issues Fixed Along the Way
- `psql: command not found` → added `postgresql-client` to Dockerfile
- `CREDENTIAL_ENCRYPTION_KEY` duplicated in docker-compose → cleaned up
- `nostr-relay_default` network not found → switched to `umbrel_main_network` (external)
- `POSTGRES_PASSWORD` not expanding (nested `${}`) → hardcoded credentials in compose
- Postgres data dir owned by root → `chmod 777`
- `Cannot find module dist/phone-network-server/src/index.js` → fixed entrypoint path + tsconfig rootDir
- Container naming changed from `_` to `-` separators in newer docker compose

### Key Config Values (Umbrel)
- **API_KEY**: `24018503cf6bd0ae1e5b1f829a5f66cf23fc2160aea90dff3b91427419cec746`
- **JWT_SECRET**: `88e43882eb6af62fc45ee602ed1b2d91789e81e695071c7e0740587c7b12023f`
- **CREDENTIAL_ENCRYPTION_KEY**: `d3b10ccdd00f9c5fff603deeb009f9e893a53b0a2781a0ee01740ade8c838c52`
- **Dashboard**: admin / `62fa93907efb78288aaa826d0a8dd6f1`
- **DB**: `phoneNetwork:phoneNetwork_umbrel@phone-network-db:5432/phoneNetwork`

### Umbrel Docker Network
- All Umbrel apps share `umbrel_main_network` — no per-app networks

### GitHub Repos
- **Public (Umbrel store)**: `enkumber/slavegate` — docker-compose, umbrel-app.yml
- **Private (server source)**: `enkumber/slavegate-server` — Dockerfile, GitHub Actions for GHCR

## Update 08:26 UTC — Server LIVE

- Added `redis:7-alpine` container → server now passes bootstrap
- Server fully starts: parser-registry, skill-updater, ops-monitor, all init
- New keypair: `32f8ea92...`

## Update 12:00-13:18 UTC — Dashboard + Relay Migration

- Dashboard included in Docker image, accessible at `http://192.168.50.57:3000`
- Dashboard base URL hardcoded fix via sed — needs permanent Dockerfile fix
- Public relays (`wss://relay.damus.io` + `wss://nos.lol`) configured
- Android auto-discovery created (no QR/manual enrollment)

## Update 17:00-17:54 UTC — DECIZIE MAJORĂ: Direct WebSocket (Eliminare Nostr)

### Migrare la DirectWs
- Dan a cerut eliminare completă Nostr din flow — direct WebSocket telefon→server
- Transport abstraction layer: `src/transport/transport.ts` — DirectWs prioritar
- `TRANSPORT_MODE=direct` în docker-compose
- Android: Nostr eliminat din UI + service, doar DirectWsClient

### Enrollment Flow Nou (DirectWs)
- Telefonul trimite fingerprint (ANDROID_ID) → server creează device pending → admin aprobă
- La reconectări: deviceId + deviceKey salvat în SharedPreferences

### Critical Bugs Fixed
1. **sendJob ignora job.jobId** — genera alt UUID intern
2. **JOB_RESULT nu updata DB** — TODO Sprint 3 niciodată implementat
3. **waitForJobResult** — adăugat registerWaiter() Promise-based
4. **jobs_job_type_check constraint** — eliminat permanent
5. **waitForResult lipsea câmpul `status`** — `pendingJobs.resolve()` returna fără `status` dar codul verifica `result?.status === "completed"` → mereu fail. **Bug-ul principal care bloca RustDesk!**
6. **Register waiter before sendJob** — race condition fix

### RustDesk Enable — FUNCȚIONEAZĂ pe DirectWs! ✅
- Sub-secundă per step (vs 8-10s pe Nostr relay)

### DB Manual Fixes pe Umbrel
- `device_key`, `first_seen_at`, `fingerprint` columns adăugate
- `auth_challenges` table creată
- `detection_events` table creată (lipsea, crăpa delete device)

## Update 18:00-18:33 UTC — Dashboard Delete Fix + Nostr Cleanup

### Dashboard Delete Device — "Failed to fetch"
- **Cauza:** `DELETE /api/devices/:id` → server crash (empty reply) → browser "Failed to fetch"
- **Root cause:** `detection_events` table lipsea → DELETE query crăpa cu uncaught error
- Creată tabelă `detection_events` — dar tot crăpa
- Investigat: `deleteDevice` face DELETE din multe tabele, + `getNostrAdapter()?.sendRevoked()` poate arunca eroare fatală
- Server-ul crăpa complet (zero logs după crash)

### DECIZIE: Eliminare Completă Nostr din Server
- Dan: "de ce nu scoti nostr pt totdeauna?"
- Subagent lansat pentru curățare completă Nostr din codebase server
- **Rezultat:** 20 fișiere modificate, -3119 linii, `tsc --noEmit` clean
- Subagentul a făcut timeout dar codul era gata — am făcut commit + push manual
- **Nostr complet eliminat din server** — zero referințe rămase
- Commit pushed, așteptăm GHCR build (~3 min)

### Next Steps
- [ ] Dan face `docker compose pull && down && up -d` pe Umbrel
- [ ] Re-upload APK după restart: `docker exec -u root ... wget -O /app/apk/phone-network.apk '<github_url>'`
- [ ] Test delete device din dashboard
- [ ] Verifică că DirectWs funcționează normal fără cod Nostr
- [ ] Fix `ops-monitor` flags column error
- [ ] Fix `canary_rollouts` table missing

### Open TODOs (carried forward)
- [ ] Dashboard base URL fix permanent în Dockerfile
- [ ] Telefonul raportează 0MB free storage
- [ ] DB migrații nu rulează automat pe Umbrel
- [ ] TLS pe WebSocket (wss://) — Caddy reverse proxy opțional
