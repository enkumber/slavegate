# PNEX-001 lane B

Run `scripts/evidence/PNEX-001/run-all.sh`. The harness reads the two canonical
worktrees, writes only PNEX-001-owned paths, and exits non-zero whenever any
required property or dispatch-bypass gate fails.
